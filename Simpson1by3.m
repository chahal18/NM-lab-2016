#==========================================================================# Shubham Chahal# 140020203052# Numerical Methods - Lab# Program to solve a system of equations using Gauss-Elimination Method.#==========================================================================function smp( a, b, n) t=cputime;  h = (b - a)/ n; s = f(a) + f(a + n*h); for i = 1 : n-1   if mod(i,2) == 0        s = s + 2*f(a+i*h);       else         s = s + 4*f(a+i*h);      endif   end  fprintf("Integration of 1/(1+x^2) over the limits 1 to 6 is "); fprintf("%f\n", s*h/3);endfunction f = f(x)  f = 1/(1+x*x);endt=cputime-t;fprintf("Time of Execution = %d",t);